



interface User {  //login user, register user, add user id to each media item, filter by user id for each user, generate random, clean up
    id?: number;
    username: string;
    password: string;
}


export default User